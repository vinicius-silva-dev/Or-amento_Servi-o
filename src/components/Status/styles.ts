import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    width: 80,
    height: 30,
    justifyContent: "center",
    alignItems: "center",
    gap: 5,
    borderRadius: 8
    // borderWidth: 2,
    // borderColor: "#6A46EB"
  }
})